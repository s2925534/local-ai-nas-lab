# Sample .env — public hostname, exposure managed entirely by ../synology-site-deployer
#
# This repo still does NOT implement any Cloudflare/DNS/tunnel logic itself. Every value below is
# just what this repo's containers expect once the deployer has done its part — see
# docs/deployer-integration.md and docs/reverse-proxy-domain.md. LOCAL_AI_BASE_PATH here is an
# illustrative example only; the deployer decides the real path.

LOCAL_AI_DOMAIN=ai.veloso.dev
LOCAL_AI_BASE_PATH=/path/managed/by/deployer/local-ai

OPEN_WEBUI_PORT=3000
OPEN_WEBUI_BIND_HOST=0.0.0.0

OLLAMA_PORT=11434
OLLAMA_BIND_HOST=127.0.0.1

WEBUI_NAME=Local AI NAS
ENABLE_SIGNUP=false

DEPLOY_MODE=public_behind_access
PUBLIC_EXPOSURE=true

REVERSE_PROXY_PROVIDER=deployer_managed
CLOUDFLARE_TUNNEL_ENABLED=false
TAILSCALE_ADMIN_ACCESS=true

MODEL_GENERAL_SMALL=llama3.2:3b
MODEL_GENERAL_MEDIUM=qwen2.5:7b
MODEL_CODING=qwen2.5-coder:7b
MODEL_EMBEDDING=nomic-embed-text

AUTO_PULL_MODELS=true
AUTO_PULL_MODELS_ON_BOOTSTRAP=true

PORTABLE_MODE=false
